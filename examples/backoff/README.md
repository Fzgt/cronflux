# backoff

Prints the sequence of retry delays produced by an exponential backoff policy
with a cap and jitter, using the `github.com/Fzgt/cronflux/backoff` package.

```sh
go run ./examples/backoff
```
